import type { FinalQuestionConfig } from "../types"

export const finalQuestionConfig: FinalQuestionConfig = {
  enableImageUpload: true,
  maxImages: 5,
  enableAIQuestion: true,
  fallbackQuestion: "Did you forget anything? Want to add more details?"
}
